services:
  postgres:
    image: postgres:15.1-alpine
    container_name: practice_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: practice_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5434:5432"
    volumes:
      - practice_postgres_data:/var/lib/postgresql/data
      - ./data/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - practice_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  pgweb:
    image: sosedoff/pgweb:latest
    container_name: practice_pgweb
    restart: unless-stopped
    ports:
      - "8082:8081"
    environment:
      - PGWEB_DATABASE_URL=postgres://postgres:postgres@postgres:5432/practice_db?sslmode=disable
    depends_on:
      - postgres
    networks:
      - practice_network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: practice_backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/practice_db
      - PORT=8000
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - practice_network
    volumes:
      - ./backend/src:/app/src:ro

volumes:
  practice_postgres_data:

networks:
  practice_network:
    driver: bridge
